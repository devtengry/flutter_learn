import 'package:flutter/material.dart';

class ServicesClass extends StatelessWidget {
  const ServicesClass({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
